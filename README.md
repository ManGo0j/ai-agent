# 🤖 ИИ Бухгалтерский ассистент (RAG Bot)

Пет-проект Telegram-бота, который отвечает на вопросы по законодательству, используя DeepSeek и векторную базу знаний.

## 🚀 Быстрый запуск

### 1. Подготовка окружения
* Установите [Docker](https://www.docker.com/) и Docker Compose.
* Создайте файл `.env` на основе `.env.example` и заполните:
    * `TELEGRAM_BOT_TOKEN` (от @BotFather)
    * `DEEPSEEK_API_KEY` (с сайта DeepSeek)
    * Доступы к БД (можно оставить стандартные).

### 2. Загрузка документов
* Положите ваши файлы (PDF, TXT) в папку `docs/`. 
* *Пример: Налоговый_кодекс.pdf, ПБУ_18.txt.*

### 3. Запуск инфраструктуры
Разверните контейнеры одной командой:
```bash
docker-compose up -d --build